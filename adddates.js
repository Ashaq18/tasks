const currentDate = new Date(); // Get the current date

// Add 7 days to the current date
currentDate.setDate(currentDate.getDate() + 7);

console.log(currentDate); // This will show the date 7 days from today
